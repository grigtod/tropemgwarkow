const config = {
    targetLat: 50.4445, // Target center lat
    targetLon: 18.8554, // Target cnter lon
    radiusMeters: 8000, // Panning lock around center
    startZoom: 18,
    minZoom: 14,
    maxZoom: 20
};

export default config;